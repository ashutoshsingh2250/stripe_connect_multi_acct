# Render Deployment Environment Variables
# Copy these to your Render service environment variables

# Server Configuration
NODE_ENV=production
PORT=10000

# JWT Configuration
JWT_SECRET=your-jwt-secret-here

# SMTP Configuration (for email exports)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Client URL (update with your actual Render domain)
CLIENT_URL=https://your-app-name.onrender.com

# Stripe Configuration (these will be sent from frontend)
# No need to set these in Render - they're encrypted and sent per request
