# 🚀 Stripe Connect Reporting API - Environment Configuration
# Copy this file to .env and fill in your actual values

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================
NODE_ENV=development
PORT=5000

# =============================================================================
# JWT CONFIGURATION
# =============================================================================
# Secret key for JWT token encryption
# In production, use a strong, randomly generated secret
JWT_SECRET=stripe-connect-jwt-secret-2025

# =============================================================================
# SMTP CONFIGURATION (Gmail Example)
# =============================================================================
# SMTP server host
SMTP_HOST=smtp.gmail.com

# SMTP server port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# Your email address
SMTP_USER=your-email@gmail.com

# Your email app password (NOT your regular password)
# Generate this in Google Account → Security → 2-Step Verification → App passwords
SMTP_PASS=your-16-character-app-password

# =============================================================================
# CLIENT CONFIGURATION
# =============================================================================
# Frontend URL for CORS configuration
# Development: http://localhost:3000
# Production: https://your-domain.com
CLIENT_URL=http://localhost:3000

# =============================================================================
# STRIPE CONFIGURATION
# =============================================================================
# Note: Stripe API keys are NOT stored in environment variables
# They are encrypted by the frontend and sent with each request
# This provides better security as keys are not stored on the server

# =============================================================================
# PRODUCTION OVERRIDES
# =============================================================================
# For production deployment, you may want to create .env.production:
# NODE_ENV=production
# PORT=10000
# CLIENT_URL=https://your-production-domain.com
# JWT_SECRET=your-production-jwt-secret

# =============================================================================
# OPTIONAL CONFIGURATIONS
# =============================================================================
# Log level (debug, info, warn, error)
# LOG_LEVEL=info

# Request timeout in milliseconds
# REQUEST_TIMEOUT=600000

# CORS allowed origins (comma-separated)
# CORS_ORIGINS=http://localhost:3000,https://your-domain.com
