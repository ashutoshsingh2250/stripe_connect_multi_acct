# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=stripe-connect-jwt-secret-2025

# Stripe keys are now provided from frontend (encrypted)
# STRIPE_SECRET_KEY and STRIPE_PUBLISHABLE_KEY are no longer needed in .env

# Email Configuration (for email export functionality)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# Timezone
DEFAULT_TIMEZONE=America/New_York
